[versions]

# Enviroment Properties
fabric-loader = "0.18.4"

# Gradle Plugin Properties
fabric-loom = "1.15-SNAPSHOT"
preprocessor = "c5abb4fb12"
grgit = "5.3.3"
lombok = "8.7.1"
replacetoken = "1.1.3"

[libraries]

fabric-loader = { group = "net.fabricmc", name = "fabric-loader", version.ref = "fabric-loader" }

[plugins]

fabric-loom = { id = "net.fabricmc.fabric-loom", version.ref = "fabric-loom" }
fabric-loom-remap = { id = "net.fabricmc.fabric-loom-remap", version.ref = "fabric-loom" }
preprocessor = { id = "com.replaymod.preprocess", version.ref = "preprocessor" }
grgit = { id = "org.ajoberstar.grgit", version.ref = "grgit" }
lombok = { id = "io.freefair.lombok", version.ref = "lombok" }
replacetoken = { id = "top.hendrixshen.replace-token", version.ref = "replacetoken" }
